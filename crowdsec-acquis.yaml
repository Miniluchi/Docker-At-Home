# Logs Traefik
filenames:
  - /var/log/traefik/access.log
labels:
  type: traefik
---
# Logs SSH
filenames:
  - /var/log/auth.log
labels:
  type: syslog
---
# Logs Authentik via Docker
source: docker
container_name:
  - authentik-server
labels:
  type: authentik
